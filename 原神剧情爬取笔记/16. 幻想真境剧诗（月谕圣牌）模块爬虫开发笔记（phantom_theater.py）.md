## 幻想真境剧诗（月谕圣牌）模块爬虫开发笔记（`phantom_theater.py`）

> 本文档记录了《原神》观测枢幻想真境剧诗（月谕圣牌）爬虫模块的设计思路、API 分析、数据结构解析和实现细节，适用于项目维护和后续扩展。


### 一、模块概述

**模块名称**：`phantom_theater.py`  
**功能**：从米哈游观测枢 Wiki 爬取所有“幻想真境剧诗”玩法中的“月谕圣牌”描述文本，包括：
- 圣牌名称（如「月谕圣牌·一·魔法师」）
- 圣牌描述/诗歌（每张牌包含一段诗意的文字，具有 Lore/文化价值）

**输出**：每张圣牌保存为一个 `.txt` 文本文件，存放在 `data/cleaned/phantom_theater/` 目录下。

**增量策略**：
- 使用 `crawled.json` 记录已爬取的圣牌 ID。
- 每次运行先检查 ID 是否已记录，若已记录则跳过，避免重复请求。


### 二、技术栈

| 组件                    | 用途                                           |
| :---------------------- | :--------------------------------------------- |
| Python 3.12+            | 运行环境                                       |
| `requests` + `tenacity` | HTTP 请求与自动重试                            |
| `json`                  | 解析 API 响应                                  |
| `logging`               | 日志记录（文件 + 控制台）                      |
| 项目内部模块            | `client`, `parser`, `config_loader`, `storage` |


### 三、API 分析

#### 3.1 圣牌列表 API

- **URL**: `https://act-api-takumi-static.mihoyo.com/common/blackboard/ys_obc/v1/home/content/list`
- **参数**: `app_sn=ys_obc`, `channel_id=249`（幻想真境剧诗频道）
- **响应结构**:
  ```
  data.data.list[0] → name: "幻想真境剧诗"
      └── list[] → 每个条目
          ├── content_id: 条目 ID
          ├── title: 条目名称
          ├── summary: 简介
          └── ext: 包含分类标签的 JSON 字符串
  ```

#### 3.2 圣牌详情 API

- **URL**: `https://act-api-takumi-static.mihoyo.com/hoyowiki/genshin/wapi/entry_page`
- **参数**: `app_sn=ys_obc`, `entry_page_id={圣牌ID}`, `lang=zh-cn`
- **核心字段**:
  - `page.modules[].components[].data`：包含 `multi_table` 组件。
  - `tables[].row[][1]`：第二列包含圣牌的标题和诗歌文本。

#### 3.3 特殊说明

- 幻想真境剧诗频道的列表数据中，包含多种类型的条目（月谕圣牌、演出事件、演出奖励、演出规则等）。
- 本模块**只保留“类别/月谕圣牌”的条目**，其他类型全部跳过。
- 圣牌的描述文本以**诗歌/塔罗牌释义**的形式呈现，具有 Lore 价值，是剧情文本的重要补充。


### 四、数据结构与解析逻辑

#### 4.1 列表 `ext` 字段解析

`ext` 是一个 JSON 字符串，包含条目的分类标签：

```json
{
  "c_249": {
    "filter": {
      "text": "[\"类别/月谕圣牌\"]"
    }
  }
}
```

解析后得到 `category: "月谕圣牌"`，用于过滤非圣牌条目。

#### 4.2 详情页“圣牌描述”解析

圣牌详情页的 `multi_table` 组件结构：

```json
{
  "component_id": "multi_table",
  "data": {
    "tables": [{
      "row": [
        ["<img...>", "<h1>一·魔法师</h1><p>意志如甘美的茶水般流涌...</p>"]
      ]
    }]
  }
}
```

**提取逻辑**：
1. 遍历 `modules`，查找 `multi_table` 组件。
2. 遍历 `tables`，取每个 `row` 的**第二列（索引 1）**的 HTML 文本。
3. 使用 `clean_html_to_text` 清洗 HTML，保留诗歌格式。
4. 如果一行只有一列（可能某些牌结构不同），则取第一列（索引 0）。

**输出格式**：每段描述用空行分隔，保留原诗的排版风格。

#### 4.3 跳过模块

| 条目类型 | 是否爬取 | 原因                        |
| :------- | :------- | :-------------------------- |
| 月谕圣牌 | ✅ 爬取   | 包含诗意文本，有 Lore 价值  |
| 演出事件 | ❌ 跳过   | 纯列表/机制说明，无叙述文本 |
| 演出奖励 | ❌ 跳过   | 纯数值列表                  |
| 演出规则 | ❌ 跳过   | 纯机制说明                  |
| 本期规则 | ❌ 跳过   | 纯机制说明                  |
| 外观奖励 | ❌ 跳过   | 纯列表                      |


### 五、输出格式

每张圣牌保存为一个 `.txt` 文件，文件名使用圣牌名称（非法字符替换为 `_`）。

**内容结构示例**（以“月谕圣牌·一·魔法师”为例）：

```
卡牌名称：月谕圣牌·一·魔法师
简介：幻想真境剧诗 月谕圣牌·一·魔法师

【描述】
一·魔法师
意志如甘美的茶水般流涌，
现实因兴致与闲暇而被塑造。
杯盏、羽毛笔、餐刀与装着甜点的圆盘，
小小的秘仪，调和的四质，
令人怀念的，午后的喧闹。

你说：万物皆备于我，
但归根结底，
那一个顷刻的欢笑与「魔法」，
也不过是无尽空无中
凝聚而成的一点光。
```


### 六、核心函数说明

| 函数                                            | 职责                                                       |
| :---------------------------------------------- | :--------------------------------------------------------- |
| `parse_card_ext(ext_str)`                       | 解析 `ext.filter.text` 中的 JSON 数组，提取类别标签。      |
| `get_card_list()`                               | 获取所有圣牌列表，仅保留“类别/月谕圣牌”的条目。            |
| `parse_card_detail(page_data)`                  | 从详情页的 `multi_table` 中提取圣牌描述文本（诗歌/释义）。 |
| `generate_card_content(card_info, description)` | 将圣牌信息和描述文本格式化为纯文本内容。                   |
| `process_card(card, output_dir)`                | 请求详情 API，解析并保存单个圣牌。                         |
| `run()`                                         | 主流程，控制爬取范围和增量逻辑。                           |


### 七、配置要求

在 `config/channels.json` 中必须包含以下条目：

```json
"phantom_theater": {
    "channel_id": 249,
    "name": "幻想真境剧诗"
}
```


### 八、使用方法

#### 8.1 激活环境并运行

```bash
cd E:\projects\genshin-story-crawler
uv run python -m src.modules.phantom_theater
```

#### 8.2 控制爬取范围

修改 `run()` 中的 `test_limit` 变量：
- `test_limit = 5`：只处理前 5 张圣牌。
- `test_limit = 0` 或 `None`：处理所有圣牌。


### 九、日志与报告

- **日志文件**：`logs/phantom_theater.log`（包含时间戳、级别、模块、行号、消息）。
- **爬取报告**：`logs/phantom_theater_report.json`，包含统计信息：
  ```json
  {
    "total": 22,
    "processed": 22,
    "failed": [],
    "skipped": 0
  }
  ```


### 十、注意事项

1. **仅保留圣牌**：幻想真境剧诗频道下有多种条目类型（演出事件、演出奖励等），本模块仅爬取“类别/月谕圣牌”的条目。
2. **无描述则跳过**：若某张圣牌没有描述文本，该圣牌将被跳过，不生成文件。
3. **诗歌格式保留**：`clean_html_to_text` 会保留原诗的换行和排版格式，确保诗歌的可读性。
4. **文件名清洗**：圣牌名称可能包含特殊字符（如 `·`、`・`），使用 `safe_filename` 替换为下划线。
5. **空内容过滤**：若圣牌详情页无任何可提取文本，该圣牌将被跳过，不生成文件。


### 十一、与已有模块的对比

| 特性     | 圣牌模块（`phantom_theater.py`） | NPC 模块（`npc.py`）              | 动物模块（`animal.py`） |
| :------- | :------------------------------- | :-------------------------------- | :---------------------- |
| 频道 ID  | `249`                            | `20`                              | `49`                    |
| 分类标签 | 仅“类别”                         | 地区、类型、牌手挑战等            | 动物类型、是否可捕获    |
| 列表结构 | 扁平列表，需过滤                 | 扁平列表                          | 扁平列表                |
| 核心内容 | 诗歌/描述文本                    | NPC 信息、对话、商品              | 背景故事                |
| 特有组件 | `multi_table`（单行双列）        | `npc_base_info`、`collapse_panel` | `good_desc`             |
| 输出粒度 | 每张圣牌一个文件                 | 每个 NPC 一个文件                 | 每个动物一个文件        |


### 十二、后续优化方向

1. **自动识别所有圣牌**：当前通过 `ext` 中的“类别/月谕圣牌”过滤，未来如果新增“塔罗牌”等其他类别，可扩展过滤条件。
2. **多语言支持**：圣牌描述目前仅中文，未来可扩展为多语言版本。
3. **分页支持**：当前频道下条目较少（~30），无需分页。如果未来扩展，可参考其他模块的分页逻辑。


**文档版本**：1.0  
**最后更新**：2026-07-05  
**作者**：Rikum