# 背包物品模块维护笔记

> 对应 `src/modules/item.py`，频道配置键为 `item`。

物品模块覆盖范围大，负责将分类、说明、来源/材料与相关叙事信息汇总为 `item:<entry_id>`。`clean_text_compact()` 用于压缩噪声空白，但不得抹除章节或语义换行。

该模块曾受到上游 JSON 截断影响；必须使用核心客户端获取目录和详情。遇到一次 JSON 解析故障时先让请求层重试，不要把整批条目错误标记为已抓取。

物品标题可能重复或含特殊字符，因此稳定 ID 是唯一身份；RAG 读取 Manifest 的标题元数据，而不依赖文件名。
