## NPC & 商店模块爬虫开发笔记（`npc.py`）

> 本文档记录了《原神》观测枢 NPC & 商店爬虫模块的设计思路、API 分析、数据结构解析、实现细节及踩坑记录，适用于项目维护和后续扩展。


### 一、模块概述

**模块名称**：`npc.py`  
**功能**：从米哈游观测枢 Wiki 爬取所有 NPC 和商店的详细信息，包括：
- NPC 名称、地区、类型（活动/商人/其他/任务）
- 牌手挑战、对话奖励、特殊效果（心声/秘闻馆情报）
- 基本信息（性别、位置、职业、功能、编者注等）
- 相关任务列表
- NPC 对话树（`interactive_dialogue` 模块）
- 商品列表 / 锻造列表（`collapse_panel` 中的表格）

**输出**：每个 NPC 保存为一个 `.txt` 文本文件，存放在 `data/cleaned/npc/` 目录下。

**增量策略**：
- 使用 `crawled.json` 记录已爬取的 NPC ID。
- 每次运行先检查 ID 是否已记录，若已记录则跳过，避免重复请求。
- 若官方更新了内容，需手动从 `crawled.json` 中删除对应 ID，重新运行以更新。


### 二、技术栈

| 组件                    | 用途                                           |
| :---------------------- | :--------------------------------------------- |
| Python 3.12+            | 运行环境                                       |
| `requests` + `tenacity` | HTTP 请求与自动重试                            |
| `json`                  | 解析 API 响应                                  |
| `BeautifulSoup` (`bs4`) | HTML 清洗与表格解析                            |
| `logging`               | 日志记录（文件 + 控制台）                      |
| 项目内部模块            | `client`, `parser`, `config_loader`, `storage` |


### 三、API 分析

#### 3.1 NPC 列表 API

- **URL**: `https://act-api-takumi-static.mihoyo.com/common/blackboard/ys_obc/v1/home/content/list`
- **参数**: `app_sn=ys_obc`, `channel_id=20`（NPC & 商店频道）
- **响应结构**:
  ```
  data.data.list[0] → name: "NPC&商店"
      └── list[] → 每个 NPC 条目
          ├── content_id: NPC ID
          ├── title: NPC 名称
          ├── summary: 简介
          └── ext: 包含分类标签的 JSON 字符串
  ```

#### 3.2 NPC 详情 API

- **URL**: `https://act-api-takumi-static.mihoyo.com/hoyowiki/genshin/wapi/entry_page`
- **参数**: `app_sn=ys_obc`, `entry_page_id={NPC ID}`, `lang=zh-cn`
- **核心字段**:
  - `page.modules[]`：所有内容模块。
  - `modules[].components[].data`：实际数据（JSON 字符串）。
  - 常见模块与组件：
    - `npc_base_info`：NPC 基本属性（性别、位置、职业、功能、编者注）。
    - `collapse_panel` / `rich_base_info`：富文本描述或表格（商品列表、锻造列表等）。
    - `interactive_dialogue`：NPC 对话树（玩家选项与 NPC 对话）。
    - `multi_table`：结构化表格数据（备用）。
    - `map_desc`：地图展示图片（无文本，忽略）。
    - `card_group_info`：七圣召唤挑战卡片（纯图片链接，忽略）。


### 四、数据结构与解析逻辑

#### 4.1 列表 `ext` 字段解析

`ext` 是一个 JSON 字符串，包含 NPC 的分类筛选标签：

```json
{
  "c_20": {
    "filter": {
      "text": "[\"地区/蒙德\", \"类型/商人\", \"牌手挑战/有\"]"
    }
  }
}
```

解析后得到：
- `region`: "蒙德"
- `type`: "商人"
- `has_card_challenge`: "有"
- `has_dialogue_reward`: ""（无）
- `special_effect`: ""（无）

#### 4.2 详情页模块解析

| 组件 ID                | 模块名示例          | 提取内容                       | 是否提取                 |
| :--------------------- | :------------------ | :----------------------------- | :----------------------- |
| `npc_base_info`        | -                   | 性别、位置、职业、功能、编者注 | ✅ 提取（作为“基本信息”） |
| `collapse_panel`       | 商品列表 / 锻造列表 | 富文本或表格                   | ✅ 提取（表格专用解析）   |
| `rich_base_info`       | -                   | 富文本                         | ✅ 提取                   |
| `interactive_dialogue` | NPC对话             | 对话与选项                     | ✅ 提取                   |
| `multi_table`          | -                   | 结构化表格                     | ✅ 提取                   |
| `map_desc`             | NPC展示             | 图片                           | ❌ 忽略                   |
| `card_group_info`      | 七圣召唤挑战        | 卡片链接                       | ❌ 忽略                   |

**特殊过滤**：
- **同伴赠礼模块**（`module_name == "同伴赠礼"`）：包含好感套装奖励表格（原石/材料/摩拉），对剧情向 RAG 价值极低，**直接跳过**。

#### 4.3 表格解析（`collapse_panel` 中的 HTML 表格）

使用 `BeautifulSoup` 解析 HTML 表格，**正确处理 `rowspan` 和 `colspan` 属性**：

1. **第一遍**：提取所有行的单元格信息（文本、`rowspan`、`colspan`），计算最大列数。
2. **第二遍**：展开 `rowspan`/`colspan`，构建规整的二维数组。
3. **动态扩展**：当 `colspan` 导致索引超出当前行长度时，自动扩展列表，避免 `IndexError`。
4. **异常捕获**：如果表格解析失败，自动回退到 `clean_html_to_text` 简单清洗，保证程序不崩溃。

输出格式示例：
```
【商品名称】 | 【商品种类】 | 价格【摩拉】 | 【限购】
银剑 | 单手剑 | 600 | 1[每日]
佣兵重剑 | 双手剑 | 600 | 1[每日]
```

#### 4.4 对话树解析（`interactive_dialogue`）

支持两种数据结构：

**标准格式**（阿妮拉、洛恩等）：
```json
{
  "list": [
    {"root_id": "...", "child_ids": {...}, "contents": {...}}
  ]
}
```

**扁平格式**（海莉等）：
```json
{
  "root_id": "...",
  "child_ids": {...},
  "contents": {...},
  "list": null
}
```

解析逻辑：
- 如果 `list` 存在且不为空，按标准格式处理。
- 如果 `list` 为 `None` 或空，但顶层存在 `root_id` 和 `contents`，则自动构造虚拟的 `list` 项。
- 使用 `" ".join(clean.split())` 压缩对话内部的换行，确保每条对话显示在同一行。
- 不同条目之间用**空行分隔**，提高可读性。


### 五、输出格式

每个 NPC 保存为一个 `.txt` 文件，文件名使用 NPC 名称（非法字符替换为 `_`）。

**内容结构示例**（以“海莉”为例）：

```
NPC名称：海莉
地区：蒙德
类型：其他
简介：海莉

【基本信息】
性别：女
位置：晨曦酒庄
功能：暂无
职业：女仆
编者注：晨曦酒庄小八卦

【相关任务】
世界任务：
酒庄大扫除
每日委托：
酒庄大扫除之二

【NPC对话】
【上午】
【摩可】
你有没有注意过…
【海莉】
注意过什么？
【摩可】
我们的钥匙都比爱德琳小姐少一把。
【海莉】
欸…那也就是说…有秘密房间？
【摩可】
对吧！对吧！你果然很懂！
然后。那天我在给地板上蜡的时候，偶然发现二楼走廊的深处里还有一个隐藏房间！
【海莉】
真的吗！那房间果然就是…？
【摩可】
我靠近房门，只听到里面越来越近的「哒哒，哒哒…」的声音。
我感到很害怕令就要离开的时候，发现爱德琳小姐就在我身后!
【海莉】
呀——！
【摩可】
可没想到，她不仅没有责骂我，还客客气气地让我回去…
可等我回到房间才忽然想起——
那个「哒哒」的声音，和爱德琳小姐穿靴子的脚步声…不是一模一样的吗?
```


### 六、核心函数说明

| 函数                                     | 职责                                                         |
| :--------------------------------------- | :----------------------------------------------------------- |
| `get_npc_list()`                         | 获取所有 NPC 的 ID、名称和分类标签（地区/类型/牌手挑战/对话奖励/特殊效果）。 |
| `parse_npc_ext(ext_str)`                 | 解析 `ext.filter.text` 中的 JSON 数组，提取五个分类标签。    |
| `parse_interactive_dialogue(comp_data)`  | 递归解析对话树，支持两种数据结构，返回格式化后的纯文本。     |
| `parse_npc_detail(page_data)`            | 遍历 `modules`，根据组件类型提取文本；跳过“同伴赠礼”模块；对包含表格的 `collapse_panel` 进行专用解析。 |
| `generate_npc_content(npc_info, detail)` | 将基本信息与各模块内容格式化为纯文本内容。                   |
| `process_npc(npc, output_dir)`           | 请求详情 API，解析并保存单个 NPC。                           |
| `run()`                                  | 主流程，控制爬取范围和增量逻辑。                             |


### 七、配置要求

在 `config/channels.json` 中必须包含以下条目：

```json
"npc": {
    "channel_id": 20,
    "name": "NPC&商店"
}
```


### 八、使用方法

#### 8.1 激活环境并运行

```bash
cd E:\projects\genshin-story-crawler
uv run python -m src.modules.npc
```

#### 8.2 控制爬取范围

修改 `run()` 中的 `test_limit` 变量：
- `test_limit = 5`：只处理前 5 个 NPC。
- `test_limit = 0` 或 `None`：处理所有 NPC。


### 九、日志与报告

- **日志文件**：`logs/npc.log`（包含时间戳、级别、模块、行号、消息）。
- **爬取报告**：`logs/npc_report.json`，包含统计信息：
  ```json
  {
    "total": 800,
    "processed": 750,
    "failed": [
      {"id": "508884", "name": "洛恩【洞天】"}
    ],
    "skipped": 50
  }
  ```


### 十、踩坑记录与注意事项

#### 10.1 对话树 `list` 字段为 `None`

**现象**：爬取“海莉”时抛出 `TypeError: 'NoneType' object is not iterable`。

**原因**：部分 NPC（如“海莉”）的 `interactive_dialogue` 组件中，`data.list` 为 `null`，数据直接存放在顶层字段（`root_id`、`contents`）。

**解决方案**：在 `parse_interactive_dialogue` 中增加 fallback 逻辑：如果 `list` 为空或 `None`，但顶层存在 `root_id` 和 `contents`，则自动构造虚拟的 `list` 项。

#### 10.2 表格解析 `IndexError: list assignment index out of range`

**现象**：爬取“阿创【「天目锻冶屋」学徒】”时抛出 `IndexError`。

**原因**：表格中存在复杂的 `colspan` 和 `rowspan` 合并，当前解析逻辑在计算列索引时出现越界。

**解决方案**：
1. **动态扩展列数**：当 `col_idx + c` 超出 `expanded_row` 长度时，自动扩展列表，避免索引越界。
2. **异常捕获**：在表格解析外层增加 `try-except`，若解析失败则回退到 `clean_html_to_text` 简单清洗，确保程序不崩溃。

#### 10.3 其他注意事项

1. **跳过同伴赠礼模块**：`module_name == "同伴赠礼"` 的模块包含好感套装奖励表格，对剧情向 RAG 价值极低，直接跳过。
2. **文件名清洗**：NPC 名称可能包含特殊字符（如 `【】`、`「」`），使用 `safe_filename` 替换为下划线。
3. **空内容过滤**：若 NPC 详情页无任何可提取文本（如仅有图片），该 NPC 将被跳过，不生成文件。


### 十一、与已有模块的对比

| 特性     | NPC 模块（`npc.py`）                      | 委托模块（`commission.py`） | 动物模块（`animal.py`） |
| :------- | :---------------------------------------- | :-------------------------- | :---------------------- |
| 频道 ID  | `20`                                      | `55`                        | `49`                    |
| 分类标签 | 地区、类型、牌手挑战、对话奖励、特殊效果  | 委托类型、区域、有无成就    | 动物类型、是否可捕获    |
| 列表结构 | 扁平列表                                  | 扁平列表                    | 扁平列表                |
| 核心内容 | NPC 信息、对话、商品                      | 委托描述、过程、对话        | 背景故事                |
| 特有组件 | `npc_base_info`、`collapse_panel`（表格） | `multi_table`               | `good_desc`             |
| 跳过内容 | 同伴赠礼                                  | 任务奖励                    | 基础信息、攻略方法      |
| 输出粒度 | 每个 NPC 一个文件                         | 每个委托一个文件            | 每个动物一个文件        |


### 十二、后续优化方向

1. **对话树分支标记**：当前对话树平铺所有节点，未来可加入缩进标记，区分不同对话分支。
2. **商品表格结构化**：将商品列表解析为 JSON 或 Markdown 表格，为 RAG 提供更精确的数据检索。
3. **支持增量更新**：通过内容哈希检测变更，避免手动删除 ID。
4. **多线程并发**：使用线程池加速爬取（需控制并发数避免风控）。
5. **命令行参数**：支持通过 `argparse` 指定 `test_limit` 或特定的 NPC ID 列表。


**文档版本**：1.1  
**最后更新**：2026-07-04  
**作者**：Rikum