## 角色逸闻爬虫模块文档（`character_anecdote.py`）

> 本文档记录了《原神》观测枢角色逸闻（游逸旅闻）爬虫模块的设计思路、技术实现、API 分析和使用方法，适用于项目维护和后续扩展。

---

### 一、模块概述

**模块名称**：`character_anecdote.py`  
**功能**：从米哈游观测枢 Wiki 爬取所有角色的逸闻纪事（游逸旅闻），包括：
- 逸闻纪事（主要剧情故事）
- 剧情彩蛋（隐藏对话）
- 角色洞天对话（尘歌壶互动）
- 七圣召唤对局对话
- 幻想真境剧诗对话
- 生日邮件内容

**输出**：每个独立事件保存为一个 `.txt` 文本文件，存放在 `data/cleaned/anecdote/` 目录下。

**增量策略**：
- 每次运行重新请求所有角色详情页。
- 通过检查文件是否已存在来决定是否保存（已存在则跳过）。
- 若官方更新内容，需手动删除对应 `.txt` 文件，重新运行以更新。

---

### 二、技术栈

| 组件                                   | 用途                                |
| -------------------------------------- | ----------------------------------- |
| Python 3.12+                           | 运行环境                            |
| `requests` + `tenacity`                | HTTP 请求与自动重试                 |
| `json`                                 | 解析 API 响应                       |
| `BeautifulSoup` (`clean_html_to_text`) | HTML 清洗                           |
| `logging`                              | 日志记录（文件 + 控制台）           |
| 项目内部模块                           | `client`, `parser`, `config_loader` |

---

### 三、数据流与核心函数

#### 3.1 整体流程

```
1. get_anecdote_character_list()
   ↓
2. 遍历每个角色 (run → process_character)
   ↓
3. 请求角色详情 API (process_character)
   ↓
4. parse_anecdotes_from_page() 解析所有内容项
   ↓
5. 生成文件名并保存 (增量检查)
```

#### 3.2 关键函数说明

| 函数                                                         | 职责                                                         |
| ------------------------------------------------------------ | ------------------------------------------------------------ |
| `get_anecdote_character_list()`                              | 获取所有包含逸闻的角色条目（ID + 名称）。                    |
| `parse_anecdotes_from_page(page_data)`                       | 解析角色详情页 JSON，提取所有内容项（逸闻、彩蛋、洞天等）。  |
| `process_character(character_id, character_name, output_dir)` | 处理单个角色，提取并保存所有内容。                           |
| `parse_interactive_dialogue(components)`                     | 解析对话树（`interactive_dialogue`），深度优先遍历，保证顺序。 |
| `safe_filename(name)`                                        | 清洗非法字符，生成安全的文件名。                             |
| `run()`                                                      | 主流程，控制爬取范围和统计。                                 |

---

### 四、API 分析

#### 4.1 角色逸闻列表 API

- **URL**: `https://act-api-takumi-static.mihoyo.com/common/blackboard/ys_obc/v1/home/content/list`
- **参数**: `app_sn=ys_obc`, `channel_id=261`（角色逸闻频道）
- **响应结构**:
  ```
  data.data.list[0] → name: "角色逸闻"
      └── list[] → 每个角色条目
          ├── content_id: 角色逸闻页面 ID
          └── title: "角色名【逸闻】"
  ```

#### 4.2 角色逸闻详情 API

- **URL**: `https://act-api-takumi-static.mihoyo.com/hoyowiki/genshin/wapi/entry_page`
- **参数**: `app_sn=ys_obc`, `entry_page_id={角色ID}`, `lang=zh-cn`
- **核心字段**:
  - `page.modules[]`：所有内容模块（基本信息、逸闻、彩蛋等）。
  - `page.template_layout.tab[]`：定义页面布局和模块分组。
    - `tab_name`：标签页名称（如“逸闻纪事”、“剧情彩蛋”等）。
    - `module_group[]`：每个分组包含多个模块 ID，对应 `modules` 中的实际内容。

---

### 五、数据结构与解析逻辑

#### 5.1 内容类型识别

通过 `template_layout.tab` 中的 `tab_name` 识别不同类型的内容：

| tab_name     | 类型标识       | 文件名前缀  | 说明             |
| ------------ | -------------- | ----------- | ---------------- |
| 逸闻纪事     | `逸闻纪事`     | （无）      | 主要逸闻故事     |
| 剧情彩蛋     | `剧情彩蛋`     | `彩蛋_`     | 隐藏对话/事件    |
| 角色洞天     | `角色洞天`     | `洞天_`     | 尘歌壶互动对话   |
| 七圣召唤     | `七圣召唤`     | `七圣召唤_` | 七圣召唤对局对话 |
| 幻想真境剧诗 | `幻想真境剧诗` | `剧诗_`     | 剧诗模式对话     |
| 生日邮件     | `生日邮件`     | `生日邮件_` | 角色生日邮件内容 |

#### 5.2 模块解析规则

每个 `module_group` 包含多个模块 ID，通过 `component_id` 区分类型：

| component_id           | 类型   | 提取内容                         |
| ---------------------- | ------ | -------------------------------- |
| `rich_base_info`       | 元信息 | 标题（名称）、简介、关键词、备注 |
| `interactive_dialogue` | 对话树 | 完整对话（按顺序）               |

**特殊处理**：
- 彩蛋的标题来自 `关键词` 字段，格式化为 `彩蛋：关键词`。
- 若缺少元信息但有对话模块，则使用模块名或标签名作为标题。
- 地图展示、角色展示等辅助模块被跳过。

#### 5.3 对话树遍历 (`parse_interactive_dialogue`)

对话树结构包含：
- `root_id`：根节点 ID。
- `child_ids`：节点之间的连接关系（支持分支）。
- `contents`：每个节点的内容（`option` 玩家选项，`dialogue` 角色台词）。

遍历策略：
1. 从 `root_id` 开始深度优先遍历。
2. 先输出 `option`（玩家选项），再输出 `dialogue`（角色台词）。
3. 递归访问所有子节点，直到叶子节点。

---

### 六、输出格式与文件命名

#### 6.1 文件命名规则

```
{角色名}_{前缀}{标题}.txt
```

- 角色名：从 `title` 中去掉 `【逸闻】` 后缀。
- 前缀：根据内容类型添加（见 5.1）。
- 标题：从元信息中提取的“名称”或“关键词”，或模块名/标签名。
- 非法字符（`\ / : * ? " < > |`）替换为 `_`。

#### 6.2 文件内容格式

```text
# {标题}
简介：{简介内容}

{对话内容（若存在）}
```

示例：
```text
# 卡齐娜·第一步
简介：在纳塔，你遇见了正在帮玛拉妮看店的卡齐娜...

卡齐娜：首先要保持微笑，等客人来的时候，要打招呼…
...
```

---

### 七、配置要求

在 `config/channels.json` 中必须包含以下条目：

```json
"anecdote": {
    "channel_id": 261,
    "name": "角色逸闻"
}
```

---

### 八、使用方法

#### 8.1 激活环境并运行

```bash
cd E:\projects\genshin-story-crawler
.venv\Scripts\activate
python -m src.modules.character_anecdote
```

#### 8.2 测试单个角色

在 `if __name__ == "__main__":` 块中调用：

```python
process_character("508214", "卡齐娜【逸闻】")
```

#### 8.3 控制爬取范围

修改 `run()` 中的 `test_limit` 变量：
- `test_limit = 20`：只处理前 20 个角色。
- `test_limit = None` 或 `0`：处理所有角色。

---

### 九、日志与报告

- **日志文件**：`logs/anecdote.log`（包含时间戳、级别、模块、行号、消息）。
- **爬取报告**：`logs/anecdote_report.json`，包含统计信息：
  ```json
  {
    "total_characters": 63,
    "processed": 60,
    "failed": [],
    "total_new_anecdotes": 0
  }
  ```

---

### 十、注意事项

1. **增量更新限制**：若官方更新了已保存的逸闻内容，需手动删除对应的 `.txt` 文件，重新运行才会更新。
2. **请求频率**：每次请求间隔 1~2 秒，避免触发反爬。若需加速，可适当缩短间隔。
3. **网络超时**：如遇 SSL 超时，可检查网络连接或增加 `timeout` 参数。
4. **文件名长度**：某些标题过长可能超出 Windows 路径长度限制（260 字符），但目前未见问题。
5. **模块扩展**：如需增加新类型的标签（如“角色邀约”），只需在 `target_tabs` 列表中添加即可。

---

### 十一、后续优化方向

1. **支持命令行参数**：通过 `argparse` 控制爬取范围和模式。
2. **统计精确化**：让 `process_character` 返回保存数量，更新 `total_new_anecdotes`。
3. **内容变更检测**：通过哈希值检测内容变化，替代手动删除文件。
4. **多线程并发**：使用 `ThreadPoolExecutor` 加速（需控制并发数）。

---

**文档版本**：1.0  
**最后更新**：2026-06-29  
**作者**：Rikum