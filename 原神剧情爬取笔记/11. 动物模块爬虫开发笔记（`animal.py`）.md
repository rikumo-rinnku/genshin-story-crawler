# 动物模块维护笔记

> 对应 `src/modules/animal.py`，频道配置键为 `animal`。

模块提取动物目录标签、资料、描述和背景文本，输出 `animal:<entry_id>`。筛选标签（地区、分类等）应作为正文上下文保留，便于检索，但不参与主键。

与所有普通条目模块一致：目录取 ID，详情取内容，富文本清洗后通过 `save_document()` 写入，成功后才 `mark_crawled()`。旧的 `safe_filename()` 只用于兼容，不应成为新语料命名策略。

复核时关注空详情、分类解析失败和目录中新增但未写入的 ID。
