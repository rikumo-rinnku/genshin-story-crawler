# 食物模块维护笔记

> 对应 `src/modules/food.py`，频道配置键为 `food`。

模块将目录标签、食物说明、配方与变体信息整理为单条纯文本，身份为 `food:<entry_id>`。变体属于同一食物条目的内容，不应因标题相近而拆成不稳定文件名。

富文本材料字段先通过公共清洗器转为正文；目录 `ext` 仅提供地区、分类等辅助信息。成功保存后再标记增量状态。若存在重复中文名称，以官方 ID 区分，不可用标题去重。

更新完毕检查模块报告中的失败条目，并执行 Manifest 校验后再交给下游。
