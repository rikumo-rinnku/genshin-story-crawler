# 敌人模块维护笔记

> 对应 `src/modules/enemy.py`，频道配置键为 `enemy`。

模块读取敌人目录与详情，展开可折叠说明后提取名称、分类、资料与背景文本，发布 `enemy:<entry_id>`。富文本中的细节面板需要先展开再清洗，避免丢失折叙信息。

`crawled.json` 只控制默认增量；当官方修改既有条目或某 ID 曾失败时，不能仅凭“已抓取”判断正确。应记录目标 ID，检查详情接口，再定向覆盖并验证正文哈希变化。

若目录存在而详情无可用正文，保留失败记录供人工复核，切勿写入空文本或伪造 `ready` 记录。
