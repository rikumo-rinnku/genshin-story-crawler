## 组织模块爬虫开发笔记（`organization.py`）

> 本文档记录了《原神》观测枢组织爬虫模块的设计思路、API 分析、数据结构解析和实现细节，适用于项目维护和后续扩展。

---

### 一、模块概述

**模块名称**：`organization.py`  
**功能**：从米哈游观测枢 Wiki 爬取所有组织的详细信息，包括：
- 组织简介（`good_desc` 模块）
- 重要事件（`timeline_base_info` 模块）
- 成员列表、下属机构、历史逸闻等（`collapse_panel` 模块）
- 额外属性（如“魔神战争”、“尘世七执政”等）

**输出**：每个组织保存为一个 `.txt` 文本文件，存放在 `data/cleaned/organization/` 目录下。

**增量策略**：
- 使用 `crawled.json` 记录已爬取的组织 ID。
- 每次运行先检查 ID 是否已记录，若已记录则跳过，避免重复请求。
- 若官方更新了内容，需手动从 `crawled.json` 中删除对应 ID，重新运行以更新。

---

### 二、技术栈

| 组件                    | 用途                                           |
| ----------------------- | ---------------------------------------------- |
| Python 3.12+            | 运行环境                                       |
| `requests` + `tenacity` | HTTP 请求与自动重试                            |
| `json`                  | 解析 API 响应                                  |
| `BeautifulSoup` (`bs4`) | 解析引用嵌套 HTML，提取完整显示名称            |
| `logging`               | 日志记录（文件 + 控制台）                      |
| 项目内部模块            | `client`, `parser`, `config_loader`, `storage` |

---

### 三、API 分析

#### 3.1 组织列表 API

- **URL**: `https://act-api-takumi-static.mihoyo.com/common/blackboard/ys_obc/v1/home/content/list`
- **参数**: `app_sn=ys_obc`, `channel_id=255`（组织频道）
- **响应结构**:
  ```
  data.data.list[0] → name: "组织"
      └── list[] → 每个组织条目
          ├── content_id: 组织 ID
          ├── title: 组织名称
          └── ext: 包含地区信息的 JSON 字符串
  ```

#### 3.2 组织详情 API

- **URL**: `https://act-api-takumi-static.mihoyo.com/hoyowiki/genshin/wapi/entry_page`
- **参数**: `app_sn=ys_obc`, `entry_page_id={组织ID}`, `lang=zh-cn`
- **核心字段**:
  - `page.modules[]`：所有内容模块。
  - `modules[].components[].data`：实际数据（JSON 字符串）。
  - 关键模块：
    - `good_desc`：简介（含 `rich_text` 和 `attr` 字段）。
    - `timeline_base_info`：重要事件（时间线）。
    - `collapse_panel`：可折叠面板，包含成员列表、下属组织、逸闻等。

---

### 四、数据结构与解析逻辑

#### 4.1 列表 `ext` 字段解析

`ext` 是一个 JSON 字符串，包含组织的筛选标签，用于提取地区信息：

```json
{
  "c_255": {
    "filter": {
      "text": "[\"地区/蒙德\"]"
    }
  }
}
```

解析后得到地区名称（如 `"蒙德"`）。

#### 4.2 详情页模块解析

| 模块名     | `component_id`       | 提取内容                                   | 是否提取 |
| ---------- | -------------------- | ------------------------------------------ | -------- |
| 简介       | `good_desc`          | 简介文本 + `attr` 键值对（如“魔神战争”）   | ✅ 提取   |
| 重要事件   | `timeline_base_info` | 事件标题和描述（时间线）                   | ✅ 提取   |
| 可折叠面板 | `collapse_panel`     | 成员列表、下属机构、逸闻等（按模块名分类） | ✅ 提取   |
| 基础信息   | `map_desc`           | 图片集                                     | ❌ 跳过   |

#### 4.3 引用替换（核心难点）

观测枢的「详情」脚注以嵌套 HTML 的形式存储在 `data-name` 属性中，例如：

```html
<span data-type="详情" data-name="&lt;p&gt;&lt;span class=&#34;custom-entry-wrapper&#34; data-entry-name=&#34;钟离&#34;&gt;&lt;a&gt;钟离&lt;/a&gt;&lt;/span&gt;·角色故事5&lt;/p&gt;">
```

处理步骤：
1. 正则匹配所有 `data-type="详情"` 的 `<span>`。
2. 提取 `data-name` 属性值，使用 `html.unescape` 解码 HTML 实体。
3. 用 BeautifulSoup 解析解码后的 HTML。
4. 遍历所有 `<p>` 标签，提取每个 `custom-entry-wrapper` 的完整显示文本（`<a>` 文本 + 后续文本节点，如“·神之眼”）。
5. 将所有名称合并为一个 `(相关内容：xxx yyy zzz)` 的字符串，替换原 `<span>`。

#### 4.4 跳过空模块

`collapse_panel` 中可能包含空内容或仅包含图片的占位模块，通过 `is_collapse_panel_empty` 函数判断（去除标签后文本长度 < 10 字符视为空）。

---

### 五、输出格式

每个组织保存为一个 `.txt` 文件，文件名使用组织名称（非法字符替换为 `_`）。

**内容结构示例**：

```
组织名称：魔神
地区：跨国家

【简介】
依据目前游戏中现有的文本(相关内容：慈水怒涛之翼 那维莱特·神之眼 钟离·角色故事5)，魔神的诞生可能与原初的碎片有着密切的关系……

魔神战争：魔神们为了争夺七个天空岛上的神座掀起的争斗。
尘世七执政：魔神战争的七位胜利者，划分尘世、各自统治七分之一。

【重要事件】
安德留斯：
与最初的天使告别后，玻瑞亚斯自挪德卡莱远赴蒙德……

奔狼的领主：
千年前，一位流浪骑士惊扰了安德留斯的沉眠……

【尘世七执政】
风神：...
岩神：...
...
```

---

### 六、核心函数说明

| 函数                                              | 职责                                                         |
| ------------------------------------------------- | ------------------------------------------------------------ |
| `get_organization_list()`                         | 获取所有组织的 ID、名称和地区信息。                          |
| `replace_references_in_html(html)`                | 将 `data-type="详情"` 的 span 替换为 `(相关内容：xxx yyy zzz)` 格式。 |
| `parse_organization_detail(page_data)`            | 解析详情页 JSON，提取简介、事件、各章节内容。                |
| `generate_organization_content(org_info, detail)` | 将解析结果格式化为纯文本内容。                               |
| `process_organization(org, output_dir)`           | 请求详情 API，解析并保存单个组织。                           |
| `run()`                                           | 主流程，控制爬取范围和增量逻辑。                             |

---

### 七、配置要求

在 `config/channels.json` 中必须包含以下条目：

```json
"organization": {
    "channel_id": 255,
    "name": "组织"
}
```

---

### 八、使用方法

#### 8.1 激活环境并运行

```bash
cd E:\projects\genshin-story-crawler
.venv\Scripts\activate
python -m src.modules.organization
```

#### 8.2 控制爬取范围

修改 `run()` 中的 `test_limit` 变量：
- `test_limit = 20`：只处理前 20 个组织。
- `test_limit = None` 或 `0`：处理所有组织。

---

### 九、日志与报告

- **日志文件**：`logs/organization.log`（包含时间戳、级别、模块、行号、消息）。
- **爬取报告**：`logs/organization_report.json`，包含统计信息：
  ```json
  {
    "total": 34,
    "processed": 30,
    "failed": [
      {"id": "507361", "name": "教令院"}
    ],
    "skipped": 4
  }
  ```

---

### 十、注意事项

1. **引用处理的特殊性**：组织模块的引用需要从 `data-name` 属性中提取，这是与角色、武器等模块不同的地方，因此引用处理逻辑放在 `organization.py` 内部，而非全局 `parser.py`。
2. **`attr` 字段**：`good_desc` 中除 `rich_text` 外，还可能包含 `attr` 键值对，需要一并提取并追加到简介末尾。
3. **空模块过滤**：`collapse_panel` 中可能包含空内容，通过 `is_collapse_panel_empty` 过滤，避免生成空白段落。
4. **网络超时**：如遇 SSL 超时，可检查网络连接或增加 `timeout` 参数。
5. **文件名清洗**：组织名称可能包含特殊字符，使用 `safe_filename` 替换为下划线。

---

### 十一、后续优化方向

1. **支持命令行参数**：通过 `argparse` 控制爬取范围和模式。
2. **内容变更检测**：通过哈希值检测内容变化，替代手动删除 ID。
3. **多线程并发**：使用 `ThreadPoolExecutor` 加速（需控制并发数）。
4. **提取更细粒度的成员列表**：如果未来需要，可以从 `collapse_panel` 中解析出更结构化的成员数据。

---

**文档版本**：1.0  
**最后更新**：2026-06-29  
**作者**：Rikum