# 原神剧情语料更新交接提示词

请将以下内容作为下一个 AI 项目的上下文：

---

我刚完成 `genshin-story-crawler` 的一次语料清理、增量更新与 RAG 发布。本次数据集已经可以接入，请按以下规则处理。

## 数据来源与入口

- 项目目录：`E:\projects\genshin-story-crawler`
- 正文目录：`data/cleaned/`
- **唯一数据入口**：`data/manifest.jsonl`
- 不要自行遍历或索引 `data/cleaned/` 中的全部文件；必须以 `manifest.jsonl` 为准。

## 当前数据质量状态

- 正式可用文档：**13,350** 条
- `status=ready`：13,350 条
- `status=needs_review`：0 条
- 数据集校验错误：0 条
- 文本均已采用稳定 ID 文件名，例如：`506567.txt`、`505850__771.txt`。
- 早期按中文标题命名的历史副本已清理，不应再被检索或重新入库。

## 本次更新范围

- 任务目录已从 1,034 条更新到 1,042 条，新增任务已抓取。
- 地图文本目录已从 630 条更新到 677 条，新增地图文本已抓取。
- 物品、动物、食物、委托、洞天等模块已完成增量更新。
- 地图文本模块已恢复并接入统一运行入口，现可按官方条目 ID 持续增量更新。
- 任务互动对话已修复：兼容官方旧版与新版 `interactive_dialogue` JSON 结构，历史任务对话缺失已补抓。
- 请求层已增加“HTTP 成功但 JSON 正文不完整”时的自动重试，组织、背包物品、地图文本模块已验证恢复正常。

## RAG 入库规则

1. 只读取 `manifest.jsonl` 中 `status == "ready"` 的记录。
2. 以 `doc_id` 作为向量库稳定主键。
3. 读取记录的 `path` 指向的 UTF-8 正文文件。
4. 将 `module`、`title`、`entry_id`、`section_id`、`source_type` 一并写入向量元数据。
5. 使用 `content_hash` 做增量判断：
   - 新 `doc_id`：新增向量；
   - 同一 `doc_id` 且哈希变化：覆盖向量；
   - 旧库存在、但新 manifest 已不存在的 `doc_id`：删除旧向量。
6. 不要将文件路径、中文文件名或标题作为唯一身份标识。

## 已知边界

以下 3 个官方页面仍未成功获取，因此不在本次语料中：

- 敌人：`508501`，深古秘源机龙；
- 秘境：`650`，地脉衍出·启示之花；
- 秘境：`651`，地脉衍出·藏金之花。

它们不影响当前数据集的完整性校验和正常接入；后续可单独修复后按照 `doc_id + content_hash` 增量补入。

## 本次接入目标

请为原神剧情 AI / RAG 应用实现或执行一次基于 manifest 的增量入库，而不是重新全量扫描旧文本。完成后请报告：新增、更新、删除、跳过的向量数量，以及任意无法读取的 manifest 记录。

---
